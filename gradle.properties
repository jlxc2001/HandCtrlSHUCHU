# Required by MediaPipe Tasks because it depends on AndroidX annotations.
android.useAndroidX=true
android.enableJetifier=true

# GitHub Actions / local Gradle memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
